enum RequestType {
    readData = "Read",
    deleteData = "Delete dt",
}

enum funs {
    read = "Read now",
    delete = "delete noww",
    id = 33
}

console.log(RequestType)
console.log(RequestType.deleteData)
console.log(RequestType["readData"])